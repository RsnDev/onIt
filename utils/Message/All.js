import React from "react";
import { StyleSheet, View, Text, ImageBackground, require } from "react-native";

const All = ({ navigation }) => {
  return (
    <View>
      <Text
        style={{
          fontSize: 30,
          marginTop: 80,
          alignContent: "center",
        }}
      >
        All'friends'
      </Text>
    </View>
  );
};

export default All;

const style = StyleSheet.create({});
