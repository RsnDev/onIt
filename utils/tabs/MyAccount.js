import React from 'react'
import { StyleSheet, View, Text, ImageBackground, require } from 'react-native'

const MyAccount = ({ }) => {
    return (
        <View>
            <Text style={{ fontSize: 30, marginTop: 80, alignContent: 'center' }}>MyAccount is coming soon</Text>
        </View>
    )
}

export default MyAccount

const style = StyleSheet.create({})
