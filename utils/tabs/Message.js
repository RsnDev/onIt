import React from 'react'
import { StyleSheet, View, Text, ImageBackground, require } from 'react-native'

const Message = ({navigation }) => {
    return (
        <View>
        <Text style={{ fontSize: 30, marginTop: 80, alignContent: 'center' }}>Message is coming soon</Text>
        </View>

    )
}

export default Message

const style = StyleSheet.create({})
