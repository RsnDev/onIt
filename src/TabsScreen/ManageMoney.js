import React from "react";
import { StyleSheet, View, Text, ImageBackground, require } from "react-native";

const ManageMoney = ({}) => {
  return (
    <View>
      <Text>ManageMoney</Text>
    </View>
  );
};

export default ManageMoney;

const style = StyleSheet.create({});
