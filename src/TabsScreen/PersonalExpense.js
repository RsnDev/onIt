import React from "react";
import { StyleSheet, View, Text, ImageBackground, require } from "react-native";

const PersonalExpense = ({}) => {
  return (
    <View>
      <Text>PersonalExpense</Text>
    </View>
  );
};

export default PersonalExpense;

const style = StyleSheet.create({});
